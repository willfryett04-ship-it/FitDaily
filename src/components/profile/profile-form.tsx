"use client";
import { FormEvent, useState } from "react";
import { Button } from "@/components/ui/button";
export function ProfileForm({ initialName, initialPreferences }: { initialName: string; initialPreferences: string }) { const [displayName, setDisplayName] = useState(initialName); const [stylePreferences, setStylePreferences] = useState(initialPreferences); const [message, setMessage] = useState<string | null>(null); const [saving, setSaving] = useState(false); async function save(event: FormEvent<HTMLFormElement>) { event.preventDefault(); setSaving(true); setMessage(null); try { const response = await fetch("/api/profile", { method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ displayName, stylePreferences }) }); const result = await response.json(); if (!response.ok) throw new Error(result.error || "We could not save your profile."); setMessage("Preferences saved."); } catch (error) { setMessage(error instanceof Error ? error.message : "We could not save your profile."); } finally { setSaving(false); } } return <form onSubmit={save} className="mt-10 grid gap-5 rounded-3xl border border-[#e5ddd5] bg-[#fcfbf9] p-6"><label><span className="mb-2 block text-sm font-medium">What should we call you?</span><input value={displayName} onChange={(event) => setDisplayName(event.target.value)} maxLength={60} placeholder="Your name" className="h-11 w-full rounded-xl border border-[#d7cec5] bg-white px-3" /></label><label><span className="mb-2 block text-sm font-medium">Your style notes</span><textarea value={stylePreferences} onChange={(event) => setStylePreferences(event.target.value)} maxLength={500} placeholder="For example: I like relaxed neutral outfits, avoid heels, and need smart-casual work looks." className="min-h-32 w-full rounded-xl border border-[#d7cec5] bg-white p-3" /></label><div><Button disabled={saving} className="h-11 rounded-xl bg-[#302a25] text-white hover:bg-[#4a4037]">{saving ? "Saving…" : "Save preferences"}</Button></div>{message && <p role="status" className="rounded-xl bg-[#efe9e1] p-3 text-sm text-[#544b43]">{message}</p>}</form>; }
