"use client";
import { useState } from "react";
import { Button } from "@/components/ui/button";
type Outfit = { title: string; explanation: string; items: Array<{ id: string; name: string }> };
export function DailyOutfit() { const [outfit, setOutfit] = useState<Outfit | null>(null); const [message, setMessage] = useState<string | null>(null); const [loading, setLoading] = useState(false); async function create() { setLoading(true); setMessage(null); try { const response = await fetch("/api/outfits/generate", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ occasion: "casual" }) }); const result = await response.json(); if (!response.ok) throw new Error(result.error || "We could not create today’s look."); setOutfit(result); } catch (error) { setMessage(error instanceof Error ? error.message : "We could not create today’s look."); } finally { setLoading(false); } } return <section className="mt-4 rounded-3xl bg-[#302a25] p-7 text-white"><p className="text-xs font-medium tracking-[0.14em] text-[#d8d0c8]">TODAY’S OUTFIT</p>{outfit ? <><h2 className="mt-3 text-2xl font-semibold">{outfit.title}</h2><p className="mt-2 text-sm leading-6 text-[#d8d0c8]">{outfit.explanation}</p><p className="mt-4 text-sm">{outfit.items.map((item) => item.name).join(" · ")}</p></> : <><h2 className="mt-3 text-2xl font-semibold">Let’s make getting dressed easy.</h2><p className="mt-2 text-sm leading-6 text-[#d8d0c8]">A relaxed look, made only from your own wardrobe.</p><Button onClick={create} disabled={loading} className="mt-5 rounded-xl bg-white text-[#302a25] hover:bg-[#f1ece7]">{loading ? "Styling…" : "Create today’s outfit"}</Button></>}{message && <p role="status" className="mt-4 rounded-xl bg-[#5b3d38] p-3 text-sm">{message}</p>}</section>; }
