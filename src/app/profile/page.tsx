import Link from "next/link";
import { redirect } from "next/navigation";
import { Button } from "@/components/ui/button";
import { ProfileForm } from "@/components/profile/profile-form";
import { createClient } from "@/lib/supabase/server";
export const metadata = { title: "Profile" };
export default async function ProfilePage() { const supabase = await createClient(); const { data: auth } = await supabase.auth.getClaims(); if (!auth?.claims) redirect("/login"); const email = typeof auth.claims.email === "string" ? auth.claims.email : ""; const { data } = await supabase.from("profiles").select("display_name, style_preferences").maybeSingle(); const profile = data as { display_name: string | null; style_preferences: string } | null; return <main className="min-h-screen bg-[#f8f7f4] px-6 py-8 text-[#201d1a] sm:px-10"><header className="mx-auto flex max-w-5xl items-center justify-between"><Link href="/dashboard" className="text-xl font-semibold tracking-tight">fit daily</Link><Button asChild variant="outline"><Link href="/dashboard">Dashboard</Link></Button></header><section className="mx-auto max-w-2xl py-14"><p className="text-sm font-medium text-[#766b61]">PROFILE & SETTINGS</p><h1 className="mt-3 text-5xl font-semibold tracking-[-0.04em]">Make Fit Daily feel more like you.</h1><p className="mt-5 text-lg leading-8 text-[#6f655d]">Signed in as {email}. These notes help guide future styling features.</p><ProfileForm initialName={profile?.display_name ?? ""} initialPreferences={profile?.style_preferences ?? ""} /></section></main>; }
